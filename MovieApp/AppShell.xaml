 using MovieApp.Views;

namespace MovieApp
{
    public partial class AppShell : Shell
    {
        public AppShell()
        {
            InitializeComponent();

            Routing.RegisterRoute("moviedetail", typeof(MovieDetailPage));
            Routing.RegisterRoute("addedit", typeof(AddEditMoviePage));
        }
    }
}